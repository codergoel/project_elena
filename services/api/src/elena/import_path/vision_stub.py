"""Vision / OCR import path stub (Phase 5.1 deferred)."""


def vision_import_status_message() -> str:
    """User-facing explanation when vision OCR is unavailable."""

    return (
        "Vision-based PDF extraction is not enabled in this build. "
        "Upload a text-based résumé PDF or paste extracted text manually."
    )
